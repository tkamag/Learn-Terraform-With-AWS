# Declare the data source
data "aws_availability_zones" "azs" { # With that we will get all AZ's dynamically.
}

# # Declare the data source
# data "aws_vpc" "selected" { # With that we will get all aws_vpc  metadata dynamically.
# }
